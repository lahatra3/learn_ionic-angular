import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avotra.angular',
  appName: 'avotra',
  webDir: 'dist/avotra',
  bundledWebRuntime: false
};

export default config;
